#include <iostream>
using namespace std;
int main(){
    int arr[50];
    //int size = size of arr(arr)/sizeof(arr[0])
    int size;
    cout<<"Enter the size of the array =====";
    cin>>size;
    for(int i=0; i<= size-1; i++)
    cin>>arr[i];
}